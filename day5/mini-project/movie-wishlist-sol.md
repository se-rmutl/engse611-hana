# 🔑 เฉลย LAB Project: แอป Movie Wishlist

## ENGSE611 - Solution Key

เอกสารนี้ประกอบด้วยโค้ดและคำอธิบายสำหรับส่วน **Challenge** ทั้ง 2 ส่วนจาก Lab Project เพื่อให้นักศึกษาสามารถเปรียบเทียบและทำความเข้าใจแนวทางการแก้ปัญหา

-----

## ส่วนที่ 1: เฉลย Challenge \#1 (CSS)

### 🎯 โจทย์

1.  **เพิ่มสถานะ "ดูแล้ว"**: สร้าง CSS class ใหม่ชื่อ `.watched` เมื่อ `.movie-item` มี class นี้ ให้เปลี่ยน `border-left-color` เป็นสีเขียว (`#28a745`) และขีดฆ่าข้อความ (`text-decoration: line-through;`)
2.  **เพิ่ม Hover Effect**: ทำให้ปุ่ม "Add" ในฟอร์มมีเงา (`box-shadow`) สวยงามเมื่อนำเมาส์ไปชี้

### 💡 แนวทางการแก้ปัญหา

เราจะเพิ่ม CSS rules ใหม่เข้าไปในไฟล์ `src/index.css` เพื่อจัดการกับสไตล์ตามที่โจทย์กำหนด

### 👨‍💻 โค้ดที่ต้องเพิ่มใน `src/index.css`

คุณสามารถเพิ่มโค้ดส่วนนี้เข้าไปในไฟล์ `src/index.css` ที่มีอยู่เดิมได้เลย

```css
/* ... โค้ดเดิม ... */

/* ==================== Add Movie Form ==================== */
/* ... โค้ดเดิมสำหรับ .add-movie-form และ input ... */

.add-movie-form button:hover {
  background-color: #3a52b8;
  transform: translateY(-2px);
  /* === ส่วนที่เพิ่มสำหรับ Challenge 1.2 === */
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.4);
}

/* ==================== Movie List ==================== */
/* ... โค้ดเดิมสำหรับ h2 และ .movie-list ... */

.movie-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 15px;
  background-color: var(--bg-primary);
  border-radius: var(--border-radius);
  border-left: 4px solid var(--primary-color);
  transition: transform 0.3s, box-shadow 0.3s, border-color 0.3s; /* เพิ่ม transition ให้ border-color */
}

/* ... โค้ดเดิมสำหรับ .movie-item:hover ... */

.movie-item span {
  font-size: 1.1rem;
  /* === ส่วนที่เพิ่มเพื่อให้คลิกได้ง่ายขึ้น === */
  cursor: pointer; 
  flex-grow: 1; /* ทำให้ span ขยายเต็มพื้นที่ที่เหลือ */
  margin-right: 10px; /* เพิ่มระยะห่างจากปุ่มลบ */
}

/* === ส่วนที่เพิ่มสำหรับ Challenge 1.1 === */
.movie-item.watched {
  border-left-color: #28a745; /* เปลี่ยนสีเส้นขอบเป็นสีเขียว */
}

.movie-item.watched span {
  text-decoration: line-through; /* ขีดฆ่าข้อความ */
  color: var(--text-secondary); /* ทำให้สีข้อความจางลง */
}

/* ... โค้ดที่เหลือ ... */

```

### ✍️ คำอธิบาย

  - **`.movie-item.watched`**: Selector นี้หมายถึง "เลือก element ที่มี class `movie-item` และ class `watched` อยู่ด้วยกัน" เราจึงกำหนดสไตล์สำหรับสถานะ "ดูแล้ว" ที่นี่
  - **`.movie-item.watched span`**: เราเจาะจงไปที่ `span` ที่อยู่ใน `.movie-item.watched` เพื่อทำการขีดฆ่าข้อความและปรับสีให้จางลง
  - **`button:hover`**: เราได้เพิ่ม `box-shadow` เข้าไปใน `:hover` pseudo-class ของปุ่ม Add เพื่อให้เกิดเงาเมื่อผู้ใช้นำเมาส์ไปชี้
  - **`cursor: pointer` บน `span`**: เราเพิ่มส่วนนี้เข้าไปเพื่อบอกใบ้ให้ผู้ใช้รู้ว่าข้อความชื่อหนังสามารถคลิกได้ (สำหรับ Challenge \#2)

-----

## ส่วนที่ 2: เฉลย Challenge \#2 (JavaScript)

### 🎯 โจทย์

1.  **Implement ฟังก์ชัน "ดูแล้ว"**: สร้างฟังก์ชัน `handleToggleWatched(id)` เพื่อสลับสถานะ `watched` ของหนัง
2.  **เชื่อมต่อกับ UI**: แก้ไข JSX เพื่อเรียกใช้ฟังก์ชันและเปลี่ยน `className` ตามสถานะของหนัง

### 💡 แนวทางการแก้ปัญหา

เราจะแก้ไขไฟล์ `src/App.js` โดยการเพิ่มฟังก์ชันใหม่และปรับปรุงส่วน JSX ที่ใช้แสดงผลรายการหนัง

### 👨‍💻 โค้ดฉบับสมบูรณ์ของ `src/App.js`

นี่คือไฟล์ `App.js` ทั้งหมดที่ได้รวมเฉลยของ Challenge เข้าไปแล้ว จุดที่มีการเปลี่ยนแปลงจะมี comment `// NEW` หรือ `// UPDATED` กำกับไว้

```javascript
import React, { useState, useEffect } from 'react';

const LOCAL_STORAGE_KEY = 'movieWishlistApp.movies';

function App() {
  const [movies, setMovies] = useState([]);
  const [inputValue, setInputValue] = useState('');

  useEffect(() => {
    const storedMovies = JSON.parse(localStorage.getItem(LOCAL_STORAGE_KEY));
    if (storedMovies) {
      setMovies(storedMovies);
    }
  }, []);

  useEffect(() => {
    localStorage.setItem(LOCAL_STORAGE_KEY, JSON.stringify(movies));
  }, [movies]);

  const handleAddMovie = (e) => {
    e.preventDefault();
    if (inputValue.trim() === '') return;

    const newMovie = {
      id: Date.now(),
      text: inputValue,
      watched: false,
    };

    setMovies(prevMovies => [newMovie, ...prevMovies]);
    setInputValue('');
  };

  const handleDeleteMovie = (id) => {
    setMovies(prevMovies => prevMovies.filter(movie => movie.id !== id));
  };
  
  // ==========================================================
  // === NEW: ส่วนที่เพิ่มสำหรับ Challenge 2.1
  // ==========================================================
  const handleToggleWatched = (id) => {
    setMovies(prevMovies =>
      prevMovies.map(movie => {
        // ถ้าเจอหนังที่ id ตรงกัน
        if (movie.id === id) {
          // ให้ return object ใหม่ โดยคัดลอกค่าเดิมมาทั้งหมด (...movie)
          // แล้วเปลี่ยนค่า watched ให้เป็นตรงกันข้าม
          return { ...movie, watched: !movie.watched };
        }
        // ถ้าไม่ใช่หนังที่ต้องการแก้ไข ก็ return ตัวเดิมกลับไป
        return movie;
      })
    );
  };

  return (
    <div className="app-container">
      <header className="app-header">
        <h1>🎬 Movie Wishlist</h1>
        <p>What do you want to watch next?</p>
      </header>

      <main>
        <form className="add-movie-form" onSubmit={handleAddMovie}>
          <input
            type="text"
            value={inputValue}
            onChange={(e) => setInputValue(e.target.value)}
            placeholder="e.g., The Matrix"
          />
          <button type="submit">Add</button>
        </form>

        <section className="movie-wishlist">
          <h2>My List</h2>
          {movies.length > 0 ? (
            <ul className="movie-list">
              {movies.map(movie => (
                // UPDATED: ส่วนที่แก้ไขสำหรับ Challenge 2.2
                <li
                  key={movie.id}
                  className={`movie-item ${movie.watched ? 'watched' : ''}`}
                >
                  <span onClick={() => handleToggleWatched(movie.id)}>
                    {movie.text}
                  </span>
                  <button onClick={() => handleDeleteMovie(movie.id)}>
                    &times;
                  </button>
                </li>
              ))}
            </ul>
          ) : (
            <p className="empty-list-message">Your wishlist is empty. Add a movie to get started!</p>
          )}
        </section>
      </main>
    </div>
  );
}

export default App;
```

### ✍️ คำอธิบาย

#### 1\. ฟังก์ชัน `handleToggleWatched`

  - **หลักการสำคัญ**: เรา **ห้ามแก้ไข State เดิมโดยตรง** (`prevMovies`) แต่ต้องสร้าง Array ใหม่ขึ้นมาแทน
  - **`prevMovies.map(...)`**: เมธอด `.map()` เหมาะสมกับงานนี้ที่สุด เพราะมันจะวนลูปใน Array เดิมและคืนค่าเป็น **Array ใหม่** ที่มีขนาดเท่าเดิม
  - **`if (movie.id === id)`**: เราตรวจสอบว่าหนังตัวที่กำลังวนลูปอยู่ใช่ตัวที่เราต้องการแก้ไขหรือไม่
  - **`return { ...movie, watched: !movie.watched }`**:
      - `...movie`: คือการใช้ Spread Operator เพื่อคัดลอก Properties ทั้งหมด (`id`, `text`, `watched`) ของ `movie` object เดิมมาใส่ใน object ใหม่
      - `watched: !movie.watched`: จากนั้นเราทำการ "เขียนทับ" (overwrite) property ที่ชื่อ `watched` ด้วยค่าที่ตรงกันข้าม (ถ้าเดิมเป็น `false` ก็เปลี่ยนเป็น `true`)
  - **`return movie`**: ถ้า `id` ไม่ตรงกัน เราก็แค่ return `movie` object เดิมกลับไปใน Array ใหม่

#### 2\. การเชื่อมต่อกับ UI

  - **`className={\`movie-item ${movie.watched ? 'watched' : ''}\`}\`**:
      - เราใช้ Template Literal (backticks \`\`) เพื่อสร้าง string สำหรับ `className`
      - `movie.watched ? 'watched' : ''` คือ Ternary Operator ที่ทำงานเหมือน if-else แบบสั้นๆ
      - ถ้า `movie.watched` เป็น `true` มันจะเพิ่ม class `watched` เข้าไป ทำให้ `className` กลายเป็น `"movie-item watched"`
      - ถ้าเป็น `false` มันจะเพิ่ม string ว่างเปล่า ทำให้ `className` เป็นแค่ `"movie-item"`
  - **`<span onClick={() => handleToggleWatched(movie.id)}>`**:
      - เราเพิ่ม `onClick` event handler ให้กับ `span` ที่แสดงชื่อหนัง
      - เมื่อถูกคลิก มันจะเรียกฟังก์ชัน `handleToggleWatched` พร้อมกับส่ง `movie.id` ของหนังรายการนั้นๆ เข้าไปเป็น argument เพื่อให้ฟังก์ชันรู้ว่าต้องไปแก้ไขหนังตัวไหน

---